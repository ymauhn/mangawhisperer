"""Concrete engine implementations.

Import submodules directly (e.g. ``from mangawhisperer.engines.pdf
import PyMuPDFPageExtractor``) — this package intentionally does not
re-export them, so importing one engine never drags in another engine's
heavy dependencies.
"""
